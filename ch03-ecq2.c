#include <stdio.h>
#include <limits.h>

int main(void) {
    // main function for problem (1)
    int8_t x = 0xFF;
    printf("(1) %i\n", x * 2);
    // main function for problem (2)
    printf("(2) %i\n", (int8_t)0xFF * 2);
    return 0;
}
