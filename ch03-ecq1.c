#include <stdio.h>
#include <limits.h>

int main(void) {
    // main function for problem (1)
    long long intMax = INT_MAX;
    printf("(1) %lli\n", intMax * intMax);
    // main function for problem (2)
    printf("(2) %lli\n", (long long)INT_MAX * (long long)INT_MAX);
    return 0;
}
